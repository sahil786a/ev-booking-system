/** Local reminders / pushes require native setup — no backend endpoints used here. */

export async function notifyBookingSoonPlaceholder(): Promise<void> {
  // Requires backend Phase 2 endpoint — server-driven reminders + device registration.
}
