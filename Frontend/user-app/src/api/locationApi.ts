/**
 * Placeholder for server-side geofencing / arrival webhooks.
 * Requires backend Phase 2 endpoint.
 */
export async function reportArrivalAtStation(_stationId: number): Promise<void> {
  // Requires backend Phase 2 endpoint.
}
